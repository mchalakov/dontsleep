# Approved starter media

Place only approved company-owned or licensed source images in this folder, then add an explicit entry to `src/content/starter-media.ts`.

Raster images are optimized into `public/starter/` during development and production builds without changing the originals. SVG files are copied as-is. No file in this folder is shipped unless it is registered.
